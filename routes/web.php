<?php

use Illuminate\Support\Facades\Route;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\DB;

Route::get('fill', function() {

    // for($x = 0; $x < 500; $x++) {

    //     DB::table("products") -> insert([
    //         "num" => 5
    //     ]);

    // }

    // return DB::table("products") -> where('id', 1950) -> first();

});